#include "main.h"
/**
 * _abs - function finds absolute value of a number
 * @n: function parameter
 * Return: -n || n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
